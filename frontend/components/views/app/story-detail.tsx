import { Container, Section } from "@/components/layout";
import { Link } from "next-view-transitions";

export default function StoryDetail() {
    return (
        <Section>
            <Container>Story Detail Page</Container>
        </Section>
    );
}
