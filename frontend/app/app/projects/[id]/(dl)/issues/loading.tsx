import { Container, Section } from "@/components/layout";

export default function Loading() {
    return (
        <Section>
            <Container>
                <p>Loading...</p>
            </Container>
        </Section>
    );
}
